const propertyStatusStyles = /*css*/`
    .property-status-container {
        text-align: center;
        padding: 8px 0;
    }

    .property-status-count {
        font-size: 30px;
        color: var(--text-muted);
        font-weight: 600;
        line-height: 1.2;
    }

    .property-status-latest {
        font-size: 16px;
        color: var(--text-faint);
        margin-top: 4px;
    }

    .property-status-empty {
        font-size: 16px;
        color: var(--text-faint);
        font-style: italic;
    }

    .property-status-value {
        font-size: 30px;
        color: var(--text-muted);
        font-weight: 600;
    }
`;

export default propertyStatusStyles;
